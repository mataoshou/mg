server:
  port: 15101

eureka:
  client:
    service-url:
      defaultZone: "http://127.0.0.1:82/eureka"
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true


spring:
  application:
    name: mg-general-eurake
